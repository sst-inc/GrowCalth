import React from "react";
import { StyleSheet, Text, View } from "react-native";

const ChallengesItem = ({ housePoint, head, sub }) => {
  return (
    <View>
      <Text>hi</Text>
    </View>
  );
};

export default ChallengesItem;

const styles = StyleSheet.create({});
